rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Test aşaması — tüm okuma/yazma açık
    // Canlıya tam geçince sıkılaştırılacak
    match /{document=**} {
      allow read: if true;
      allow write: if true;
    }
  }
}
